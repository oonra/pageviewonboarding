/*


--- Screen sizes of the Nexus 6, which is the mockup emulator used for testing ---
const double mockupWidth = 412.0;
const double mockupHeight = 732.0;

--- Screen sizes of the actual device that the app will be tested on ---
final double screenWidth = MediaQuery.of(context).size.width;
final double screenHeight = MediaQuery.of(context).size.height;

--- Scaling variables which will be used for scaling sizes of texts, images, padding, border radius, etc. ---
final double widthScale = screenWidth / mockupWidth;
final double heightScale = screenHeight / mockupHeight;


*/